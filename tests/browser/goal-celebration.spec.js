import{test,expect}from'@playwright/test';
test('le but canonique affiche une séquence qui attend Continuer',async({page})=>{await page.goto('/index.html');const result=await page.evaluate(async()=>{const[{buildGoalPresentation},{default:GoalCelebrationOverlay}]=await Promise.all([import('/domain/match/goalPresentation.js'),import('/ui/goalCelebrationOverlay.js')]);let continued=false;const goal=buildGoalPresentation({eventId:'match-9:goal:2',matchId:'match-9',scorer:'Alex',minute:89,score:{home:2,away:1},gesture:'Panenka'}),overlay=new GoalCelebrationOverlay({onContinue:()=>continued=true});overlay.show(goal);const node=document.querySelector('[data-goal-celebration]');const before=Boolean(node),eventId=node?.dataset.goalEventId,cameraState=node?.dataset.cameraState,intensity=node?.dataset.emotionalIntensity;document.querySelector('[data-goal-continue]').click();return{before,eventId,cameraState,intensity,continued,after:Boolean(document.querySelector('[data-goal-celebration]'))};});expect(result.before).toBe(true);expect(result.eventId).toBe('match-9:goal:2');expect(result.cameraState).toBe('GOAL');expect(result.intensity).toBe('3');expect(result.continued).toBe(true);expect(result.after).toBe(false);});
