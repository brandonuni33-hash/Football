import test from'node:test';import assert from'node:assert/strict';import{importanceDecisionPolicy}from'../../domain/match/matchImportanceDecisionPolicy.js';test('une finale et un match normal ont des politiques différentes',()=>{assert.deepEqual(importanceDecisionPolicy({type:'final'}),{min:4,max:6,pacing:'reversals',pressureEligible:true});assert.equal(importanceDecisionPolicy({}).pressureEligible,false);});
