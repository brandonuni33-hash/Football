import test from'node:test';import assert from'node:assert/strict';import{matchPlayerIdentity}from'../../domain/match/matchPlayerIdentity.js';test('le futsal conserve une empreinte petit espace',()=>{const p=matchPlayerIdentity({origin:'FUTSAL',attributes:{controle:80,dribble:84}});assert.equal(p.origin,'FUTSAL');assert.equal(p.technique,82);assert.equal(p.smallSpaceAffinity,1);});
