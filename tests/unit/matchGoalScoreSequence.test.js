import test from'node:test';import assert from'node:assert/strict';import{goalScoreSnapshots}from'../../domain/match/matchGoalScoreSequence.js';test('chaque but affiché reste compatible avec le score final',()=>{const s=goalScoreSnapshots({matchId:'m',goals:2,teamGoals:3,opponentGoals:1,home:true});assert.equal(s.length,2);assert.equal(s.at(-1).score.home,3);assert.ok(s.every(x=>x.score.home<=3&&x.score.away<=1));});
