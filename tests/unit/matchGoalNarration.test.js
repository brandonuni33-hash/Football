import test from'node:test';import assert from'node:assert/strict';import{goalNarration}from'../../domain/match/matchGoalNarration.js';test('la narration de but reprend minute score et geste',()=>{const n=goalNarration({scorer:'Alex',minute:82,gesture:'Extérieur du pied',score:{home:2,away:1}});assert.match(n.text,/82/);assert.match(n.text,/2–1/);assert.match(n.text,/Extérieur/);});
