import test from'node:test';import assert from'node:assert/strict';import{buildInteractiveMatchReport}from'../../domain/match/interactiveMatchReport.js';import{buildMatchBlockPresentation}from'../../domain/match/matchBlockPresentation.js';test('un match simulé ne devient jamais Ton match',()=>{const simulated={played:true,interactive:false,rating:9,goals:3};assert.equal(buildInteractiveMatchReport(simulated),null);assert.equal(buildMatchBlockPresentation([simulated]).playedMatch,null);});
