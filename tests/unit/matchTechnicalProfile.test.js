import test from'node:test';import assert from'node:assert/strict';import{technicalProfile}from'../../domain/match/matchTechnicalProfile.js';test('le profil lit les vraies stats techniques',()=>{const p=technicalProfile({origin:'FUTSAL',attributes:{controle:90,dribble:85,passe:70,tir:60}});assert.equal(p.origin,'FUTSAL');assert.ok(p.technique>80);assert.equal(p.futsalBonus,.1);});
