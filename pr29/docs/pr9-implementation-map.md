# PR9 — Carte exigences → code

1. contrôle manuel → `interactiveMatchNarrative`, `interactiveMatchFlowController`.
2. structure variable + chrono exceptionnel → `interactiveMatchController`, `matchDecisionPlan`, `matchPressurePolicy`.
3. cinq conséquences → `interactiveMatchMemory`, `matchConsequenceChain`, `matchChoiceConsequenceRecord`.
4. spectacle mérité → `spectacularGestureCatalog`, `matchTechnicalProfile`, `matchGestureOutcome`, politiques altercation/célébration.
5. but visuel canonique → `goalEventResolver`, `goalPresentation`, `goalCelebrationOverlay`.
6. un seul match → `matchIdentityGuard`, `matchNarrativeInvariant`, `matchBlockInvariant`.
7. appellations → `matchBlockPresentation`, `matchReportLabels`.
8. rapport narratif → `matchStoryWriter`, `matchReportFocus`.
9. voix sociales → `matchReactionFact`, `matchReactionVoices`, `matchVoiceProfiles`.
