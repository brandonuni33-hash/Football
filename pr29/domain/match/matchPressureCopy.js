export function pressureCopy(reason='moment décisif'){if(reason==='finale')return'Le temps se resserre. En finale, cette décision ne t’attendra pas.';if(reason==='rivalité')return'Le stade hurle et le duel arrive tout de suite. Décide.';return'La fenêtre est minuscule. Tu dois choisir maintenant.';}
export default pressureCopy;
