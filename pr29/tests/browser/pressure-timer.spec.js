import{test,expect}from'@playwright/test';
test('la minuterie apparaît uniquement sur une décision marquée sous pression',async({page})=>{await page.goto('/index.html');const result=await page.evaluate(async()=>{const{default:InteractiveMatchFlowController}=await import('/ui/interactiveMatchFlowController.js');const calls=[];const controller=new InteractiveMatchFlowController({ui:{gateway:{playNextBlock:v=>{calls.push(v);return null;}}}});controller.show({interactiveStep:{phase:'moment_4',kind:'decision',label:'MOMENT',progress:80,minute:88,title:'Dernière occasion',text:'Décide.',team:'FC',opponent:'Rival',home:true,score:{home:1,away:1},choices:[{text:'Frapper'},{text:'Passer'}],timedDecision:{seconds:5}}});const timer=Boolean(document.querySelector('[data-pressure-countdown]'));controller.clear();controller.show({interactiveStep:{phase:'kickoff',kind:'narration',label:'MATCH',progress:10,minute:0,title:'Début',text:'Le match commence.',team:'FC',opponent:'Rival',home:true,score:{home:0,away:0},choices:[]}});return{timer,narrativeTimer:Boolean(document.querySelector('[data-pressure-countdown]'))};});expect(result.timer).toBe(true);expect(result.narrativeTimer).toBe(false);});
