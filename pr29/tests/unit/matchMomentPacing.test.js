import test from'node:test';import assert from'node:assert/strict';import{matchPacing}from'../../domain/match/matchMomentPacing.js';test('les rythmes diffèrent selon le match',()=>{assert.equal(matchPacing({type:'standard'}),'direct');assert.equal(matchPacing({type:'rival'}),'aggressive');assert.equal(matchPacing({type:'final'}),'reversals');assert.equal(matchPacing({started:false}),'urgent');});
