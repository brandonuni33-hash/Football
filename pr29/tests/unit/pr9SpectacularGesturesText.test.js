import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';test('le contrat couvre le catalogue spectaculaire',()=>{const t=fs.readFileSync(new URL('../../docs/pr9-spectacular-gestures.md',import.meta.url),'utf8');for(const x of['panenka','talonnade','petit pont','sombrero','extérieur','volée','tacle','tirage','célébration'])assert.match(t,new RegExp(x));});
