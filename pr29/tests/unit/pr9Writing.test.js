import test from'node:test';import assert from'node:assert/strict';import{writeMatchStory}from'../../domain/match/matchStoryWriter.js';test('le récit ne se réduit pas à note et pourcentage',()=>{const s=writeMatchStory({matchId:'m',rating:8.2,goals:0,assists:0,events:[{minute:70,text:'Ton appel fait reculer la défense.'}],decisions:[{choice:'Attaquer le dos'}]});assert.equal(/%|réussi \d+/.test(s.text),false);assert.match(s.text,/rapport de force|défense|choix/);});
