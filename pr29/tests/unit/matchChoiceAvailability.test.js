import test from'node:test';import assert from'node:assert/strict';import{choiceAvailable}from'../../domain/match/matchChoiceAvailability.js';test('un geste trop technique est masqué',()=>assert.equal(choiceAvailable({minTechnique:85},{technique:60}),false));test('un choix de penalty ne sort pas en jeu ouvert',()=>assert.equal(choiceAvailable({contexts:['penalty']},{technique:90,situation:'open_play'}),false));
