const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
export class GoalCelebrationOverlay{constructor({onContinue}={}){this.onContinue=onContinue;}show(goal){if(!goal?.matchId||goal.kind!=='goal')return false;this.clear();const node=document.createElement('div');node.dataset.goalCelebration='true';node.dataset.matchId=goal.matchId;node.className='goal-celebration-overlay';node.innerHTML=`<section role="dialog" aria-modal="true" aria-label="But de ${esc(goal.scorer)}"><span class="goal-celebration-minute">${esc(goal.minute)}'</span><h1>BUT</h1><div class="goal-celebration-score">${esc(goal.score.home)} – ${esc(goal.score.away)}</div><h2>${esc(goal.scorer)}</h2>${goal.gesture?`<strong>${esc(goal.gesture)}</strong>`:''}<p>${esc(goal.celebration)}</p><p>${esc(goal.stadiumReaction)}</p><button type="button" data-goal-continue>Continuer</button></section>`;(document.getElementById('app')||document.body).appendChild(node);node.querySelector('[data-goal-continue]').addEventListener('click',()=>{this.clear();this.onContinue?.(goal);});return true;}clear(){document.querySelector('[data-goal-celebration]')?.remove();}}
export default GoalCelebrationOverlay;
