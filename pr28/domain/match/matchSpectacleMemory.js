export function careerWorthyMatchMoment({matchId,type,gesture,success,importance,minute}={}){if(!matchId||!gesture)return null;const major=type==='final'||importance==='exceptional'||Number(minute)>=85;const iconic=['Panenka','Bicyclette','Sombrero'].some(x=>String(gesture).includes(x));if(!major&&!iconic)return null;return{matchId,kind:'spectacular-moment',gesture,success:Boolean(success),importance:major?'major':'important',memoryText:success?`${gesture} réussi dans un moment qui comptait.`:`${gesture} manqué dans un moment qui comptait.`};}
export default careerWorthyMatchMoment;
