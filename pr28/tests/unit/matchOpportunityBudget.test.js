import test from'node:test';import assert from'node:assert/strict';import{opportunityBudget}from'../../domain/match/matchOpportunityBudget.js';test('un match verrouillé peut avoir moins de décisions que la cible',()=>assert.ok(opportunityBudget({planned:4,locked:true})<4));test('il reste au moins une vraie fenêtre',()=>assert.equal(opportunityBudget({planned:1,locked:true,scoreDiff:4}),1));
