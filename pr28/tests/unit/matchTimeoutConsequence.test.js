import test from'node:test';import assert from'node:assert/strict';import{timeoutConsequence}from'../../domain/match/matchTimeoutConsequence.js';test('ne pas choisir produit une vraie conséquence',()=>{const c=timeoutConsequence({matchId:'m',minute:90,reason:'finale'});assert.ok(c.immediate);assert.ok(c.matchEvolution);assert.ok(c.statistics);assert.ok(c.delayed);assert.ok(c.narrative);});
