import test from'node:test';import assert from'node:assert/strict';import{altercationAvailable}from'../../domain/match/matchAltercationPolicy.js';test('une altercation n apparaît pas sans tension',()=>assert.equal(altercationAvailable({}).available,false));test('un derby avec duels répétés peut déclencher une altercation',()=>assert.equal(altercationAvailable({type:'rival',duels:2,defenderStress:.2}).available,true));
