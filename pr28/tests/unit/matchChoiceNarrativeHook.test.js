import test from'node:test';import assert from'node:assert/strict';import{narrativeHooksForChoice}from'../../domain/match/matchChoiceNarrativeHook.js';test('un petit pont risqué expose des hooks précis',()=>{const h=narrativeHooksForChoice({gesture:'Petit pont',impacts:{duelBonus:.2,goalChance:.1}},{});assert.ok(h.includes('spectacular-gesture'));assert.ok(h.includes('repeated-duel'));});
