export function decisionOutcomeText({gesture=null,success=true,duel=false,timedOut=false}={}){if(timedOut)return'Tu hésites une seconde de trop. Le défenseur revient et la fenêtre disparaît.';if(gesture&&success)return`${gesture} passe. Le geste n’est pas gratuit : il oblige immédiatement l’adversaire à modifier son intervention.`;if(gesture&&!success)return`${gesture} ne passe pas. Le ballon s’échappe et l’adversaire peut punir le risque pris.`;if(duel)return'Tu imposes le duel. Le défenseur sait maintenant que tu reviendras le chercher.';return'Ton choix déplace légèrement le rapport de force, sans décider encore du match.';}
export default decisionOutcomeText;
