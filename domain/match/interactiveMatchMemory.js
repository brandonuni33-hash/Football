// Mémoire locale d'un match jouable. Elle ne remplace jamais le State carrière.
const n=v=>Number.isFinite(Number(v))?Number(v):0;
export function createMatchMemory(){return{duelsAttempted:0,technicalAttempts:0,technicalFailures:0,pressureMisses:0,defenderStress:0,spaceCreated:0,riskTaken:0,lastChoice:null};}
export function recordMatchChoice(memory={},choice={},event={}){memory.duelsAttempted=n(memory.duelsAttempted)+(n(choice.impacts?.duelBonus)>0?1:0);memory.technicalAttempts=n(memory.technicalAttempts)+(choice.gesture?1:0);memory.technicalFailures=n(memory.technicalFailures)+(event.failedTechnique?1:0);memory.defenderStress=n(memory.defenderStress)+Math.max(0,n(choice.impacts?.duelBonus)*2)+(choice.gesture?.toLowerCase().includes('petit pont')?.18:0);memory.spaceCreated=n(memory.spaceCreated)+Math.max(0,n(choice.impacts?.goalChance)+n(choice.impacts?.assistChance));memory.riskTaken=n(memory.riskTaken)+Math.max(0,n(choice.impacts?.technicalRisk)+n(choice.impacts?.cardRisk));memory.lastChoice=choice.text||null;return memory;}
export function recordPressureMiss(memory={}){memory.pressureMisses=n(memory.pressureMisses)+1;return memory;}
export function delayedMatchEffect(memory={}){if(n(memory.defenderStress)>=.35)return{id:'defender_overloaded',title:'Le défenseur commence à subir',text:'À force de le provoquer, tu sens son placement changer. Il recule plus tôt et intervient avec moins de marge.',effects:{goal:.045,assist:.025,cards:.035}};if(n(memory.technicalFailures)>=2)return{id:'technical_doubt',title:'Le risque laisse une trace',text:'Deux gestes n’ont pas passé. L’adversaire attend désormais ton prochain défi et ferme plus vite le petit espace.',effects:{goal:-.025,opponentThreat:.035}};if(n(memory.pressureMisses)>0)return{id:'hesitation_echo',title:'L’hésitation reste dans un coin de ta tête',text:'La fenêtre manquée plus tôt revient au moment où le jeu accélère. Cette fois, il faudra décider plus vite.',effects:{rating:-.02}};return null;}
export default{createMatchMemory,recordMatchChoice,recordPressureMiss,delayedMatchEffect};
