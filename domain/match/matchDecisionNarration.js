export function decisionNarration({minute,opponent,memory={},index=0}={}){if(index===0)return`À la ${minute}e minute, une première brèche apparaît face à ${opponent}. Tu as le temps de lire la situation.`;if(Number(memory.defenderStress)>=.35)return`À la ${minute}e, le défenseur que tu as déjà travaillé ne sort plus de la même manière. Il protège d’abord son dos.`;if(Number(memory.pressureMisses)>0)return`À la ${minute}e, une nouvelle fenêtre s’ouvre. La précédente s’était refermée pendant ton hésitation.`;if(Number(memory.technicalFailures)>=2)return`À la ${minute}e, l’adversaire attend désormais ton geste. Pour le surprendre, il faudra changer quelque chose.`;return`À la ${minute}e minute, le match t’offre une nouvelle décision. Ce qui s’est passé avant change déjà la façon de la lire.`;}
export default decisionNarration;
